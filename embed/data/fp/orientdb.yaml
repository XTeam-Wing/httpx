info:
  category: database system
  company: OrientDB LTD.
  cpe: ''
  product: orientdb
  server: nginx
  tags: []
rules:
- dsl: (icontains(title,"redirecting to orientdb") || ((!icontains(body,"cms web viewer")
    && !icontains(banner,"x-")) && icontains(server,"orientdb")))
  method: GET
  
