info:
  category: service
  company: Google LLC
  cpe: ''
  product: google-hosted-libraries
  server: nginx
  tags: []
rules:
- dsl: ((icontains(body,"ajax.googleapis.com/ajax/libs") || icontains(banner,"ajax.googleapis.com/ajax/libs"))
    || icontains(banner,"ajax.googleapis.com/ajax/libs"))
  method: GET
  
