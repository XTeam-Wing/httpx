info:
  category: log analysis && audit
  company: Graylog, Inc.
  cpe: ''
  product: graylog
  server: nginx
  tags: []
rules:
- dsl: (((((icontains(banner,"basic realm=\"graylog \"") || icontains(banner,"x-graylog-node-id"))
    || icontains(banner,"basic realm=\"graylog \"")) || icontains(banner,"x-graylog-node-id"))
    || icontains(body,"org.graylog.plugins.pipelineprocessor.processorplugin")) ||
    icontains(title,"graylog web interface"))
  method: GET
  
