info:
  author: Loong716
  description: 'RIS-ACA齐治访问控制审计系统 was detected.'
  tags: []
  product: ris-aca齐治访问控制审计系统
  category: cms
rules:
- method: GET
  path:
  - '/shterm/login'
  dsl: contains(body, "localStorage.deployPath=\"shterm\";") || contains(body, "action=\"/shterm/login\"")|| contains(body, "pwd_fn();encrypt_passwd();form.submit();")
