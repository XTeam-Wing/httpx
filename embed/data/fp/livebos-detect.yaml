info:
  author: pen4uin
  description: LiveBos
  tags: []
  product: livebos
  category: cms
rules:
- method: GET
  path: 
    - "/login.do"
  dsl: contains(body, "LiveBOS") || contains(title, "Power by LiveBOS")