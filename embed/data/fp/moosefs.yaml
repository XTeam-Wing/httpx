info:
  category: file management && encryption
  company: other
  cpe: ''
  product: moosefs
  server: nginx
  tags: []
rules:
- dsl: (icontains(body,"under-goal files") || icontains(body,"mfs.cgi"))
  method: GET
  
