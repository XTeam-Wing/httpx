info:
  author: pen4uin
  description: XWiki is a free wiki software platform written in Java with a design emphasis on extensibility.
  remediation:
  - 保持 XWiki 为最新版本，并遵循官方安全建议。
  tags:
  - tech
  - xwiki
  - wiki
  product: xwiki
  category: cms
rules:
- method: GET
  path:
  - /bin/login/XWiki/XWikiLogin
  dsl: status_code == 401 && contains(tolower(body), "xwiki-version")
