info:
  company: Apache
  product: apache spark
  category: other
  description: Apache Spark是一个开源集群运算框架，最初是由加州大学柏克莱分校AMPLab所开发。
  author: 哆啦A梦
  tags:
  - tech
  - apache-spark
rules:
- method: GET
  path: 
  - /static/spark-logo-77x50px-hd.png
  dsl: md5(body) == "165de54ff29d30a3773c53e7911227d3"
- method: GET
  dsl: contains(body, "setUIRoot") || contains(body, "Spark Worker at") || contains(body, "Spark Master at") || contains(body, "bootstrap-tooltip.js") || contains(body, "webui.js")
