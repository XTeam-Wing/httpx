info:
  category: identity authentication && authority management
  company: Micro Focus International plc
  cpe: ''
  product: novell-ichain
  server: nginx
  tags: []
rules:
- dsl: ((((icontains(banner,"host name received is not for this web site") || icontains(banner,"host
    name received is not for this web site")) || title == "novell proxy") || title
    == "novell ichain") || icontains(body,"src=\"ichainerrors/alertbar.gif"))
  method: GET
  
