info:
  product: finereport
  category: 报表工具
  author: sayhi
  company: 帆软软件有限公司
  description: FineReport是帆软软件有限公司推出的企业级报表工具
  fofa_query: app="帆软-FineReport"
  tags:
  - detect
  - finereport
  - tech
rules:
- dsl: contains(body, 'content=\"FineReport\"') || contains(body, '/ReportServer?op=') || contains(body, '/webroot/decision') || contains(body, '<title>FineReport')
- method: GET
  path: 
    - "/webroot/ReportServer"
  dsl: contains(body, 'content=\"FineReport\"') || contains(body, '/ReportServer?op=') || contains(body, '/webroot/decision') || contains(body, '<title>FineReport')
