info:
  category: operating system
  company: Canonical Ltd.
  cpe: ''
  product: ubuntu
  server: ubuntu
  tags: []
rules:
- dsl: ((icontains(banner,"x01ubuntu") || icontains(body,"welcome to nginx on ubuntu!"))
    || icontains(banner,"ubuntu"))
  method: GET
  
