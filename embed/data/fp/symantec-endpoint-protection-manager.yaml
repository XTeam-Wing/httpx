info:
  category: other network security
  company: Symantec Corporation
  cpe: ''
  product: symantec-endpoint-protection-manager
  server: nginx
  tags: []
rules:
- dsl: '(((icontains(banner,"server: sepm") || icontains(banner,"symantec.html"))
    || icontains(banner,"symantec endpoint protection manager")) || (!icontains(body,"server:
    couchdb") && (((((icontains(banner,"symantec endpoint protection manager") ||
    icontains(banner,"server: sepm")) || icontains(body,"<tr><td align=\"left\" style=\"font-family:arial;
    font-size:18pt\"><b>symantec endpoint protection manager<br>web access</b></td></tr>"))
    || icontains(body,"<!-- now, if it is ie on windows platform, we check to see
    which version of jws is installed -->")) || icontains(body,"/portal/about.jsp"))
    || icontains(body,"<div style=\"font-family: tahoma, verdana, arial, helvetica,
    sans-serif; font-size:11px;\">version"))))'
  method: GET
  
