info:
  category: other network equipment
  company: other
  cpe: ''
  product: mrtg
  server: nginx
  tags: []
rules:
- dsl: (((((icontains(banner,"realm=\"mrtg ") || icontains(banner,"realm=\"mrtg "))
    || icontains(body,"command line is easier to read using \"view page properties\"
    of your browser")) || icontains(body,"/mrtg-l.png")) || icontains(body,"href=\"http://oss.oetiker.ch/mrtg/"))
    || icontains(title,"mrtg index page"))
  method: GET
  
