info:
  category: log analysis and audit
  company: Splunk Inc.
  cpe: ''
  product: splunk-日志分析
  server: unknown
  tags: []
rules:
- dsl: '(icontains(banner,"Server: Splunkd") && body!="Server: couchdb" && header!="drupal")
    || (icontains(banner,"Server: Splunkd") && banner!="couchdb" && banner!="drupal")
    || (icontains(body,"Splunk.util.normalizeBoolean") && body!="Server: couchdb"
    && header!="drupal")'
  method: GET
  
