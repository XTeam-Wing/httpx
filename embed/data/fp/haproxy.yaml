info:
  category: unknown
  company: Unknown
  cpe: ''
  product: haproxy
  server: Unknown
  tags: []
rules:
- dsl: icontains(title,"haproxy")||(icontains(header,"server:") && icontains(header,"haproxy on
    top of other infrastructure"))||(icontains(header,"realm=\"haproxy statistics"))||(icontains(body,"basic
    realm=\"haproxy statistics\""))
  method: GET
  
