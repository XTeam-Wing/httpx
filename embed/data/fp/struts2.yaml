info:
  category: development framework
  company: Apache Software Foundation.
  cpe: ''
  product: struts2
  server: nginx
  tags: []
rules:
- dsl: (((((icontains(body,"action") && icontains(body,"<input type=\"hidden\" name=\"struts.token.name\""))
    || title == "struts2 showcase") || icontains(body,"no result defined for action
    && result input")) || icontains(body,"there is no action mapped for namespace"))
    || icontains(body,"struts problem report"))
  method: GET
  
