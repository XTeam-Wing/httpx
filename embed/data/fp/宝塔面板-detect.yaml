info:
  author: 哆啦A梦
  description: 安全高效的服务器运维面板
  tags: []
  product: 宝塔面板
  category: cms
rules:
- dsl: contains(body, " <title>安全入口校验失败</title>") || contains(body, "请使用正确的入口登录面板") || contains(header, "宝塔面板")
- method: GET
  path: 
    - "/favicon.ico"
  dsl: status_code == 200 && (md5(body) == "9637ebd168435de51fea8193d2d89e39")