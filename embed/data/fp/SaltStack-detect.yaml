info:
  author: 哆啦A梦
  remediation:
  - SaltStack Config 由 Salt 提供支持，Salt 是由 VMware 赞助的开源自动化和配置管理引擎。Salt 由 Salt Project 社区构建，该社区包括 3,000 多位工作职能与您一样的参与者。这个众所周知且值得信赖的社区创建了各种执行和状态模块来改进底层技术并扩展了 Salt，可完成最常见的任务或解决您的角色可能面临的最重要问题。
  tags:
  - tech
  - saltstack
  product: saltstack
  category: cms
rules:
- method: GET
  path:
  - /
  dsl: md5(body) == '8259624a1bf4f9a3e817c7bf80f52db9'
- method: GET
  path:
  - /login
  dsl: status_code == 200 && contains(title,"SaltStack Config")
