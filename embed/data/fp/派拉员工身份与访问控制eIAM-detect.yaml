info:
  author: Loong716
  description: '派拉员工身份与访问控制eIAM was detected.'
  tags: []
  product: 派拉员工身份与访问控制eiam
  category: security
rules:
- method: GET
  path:
  - '/'
  dsl: contains(body, "/esc-sso/oauth2.0/authorize") && contains(body, "ParaView version") || contains(header, "para-node")
