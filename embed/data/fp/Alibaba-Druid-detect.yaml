info:
  product: alibaba druid
  category: database
  author: 哆啦A梦
  company: 阿里巴巴
  description: 阿里云计算平台DataWorks团队出品，为监控而生的数据库连接池
  tags:
  - tech
  - alibaba-druid
rules:
- method: GET
  path: 
   - "/druid/index.html"
   - "/druid/login.html"
  dsl: status_code == 200 && (contains(body, "Druid Stat Index</title>") || contains(body, "druid monitor</title>"))
