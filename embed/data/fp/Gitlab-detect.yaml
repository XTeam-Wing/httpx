info:
  company: GitLab
  product: gitlab
  category: 版本控制vcs
  description: ''
  author: 哆啦A梦
  tags:
  - tech
  - gitlab
rules:
- method: GET
  
  dsl: contains(body, "<meta content=\"GitLab\" property=\"og:site_name\">") || contains(body, "<meta content=\'GitLab\' property=\'og:site_name\'>") || contains(body, "<title>Sign in | GitLab</title>") || contains(body, "<title>登录 · GitLab</title>") || contains(header,"_gitlab_session")
