info:
  category: unknown
  company: Unknown
  cpe: ''
  product: vlc-web-interface
  server: Unknown
  tags: []
rules:
- dsl: icontains(body,"<title>vlc media player - web interface</title>")||(icontains(body,"<title>vlc
    media player - web interface - mosaic wizard</title>"))||(icontains(body,"<title>vlc
    media player - web interface with flash viewer</title>"))||(icontains(body,"<title>vlc
    media player - web interface - vlm</title>"))||(icontains(body,"<!-- do we need to
    browse in order to setup a mosaic ? for the background image maybe ... -->"))||(icontains(body,"<input
    type=\"checkbox\" id=\"vlm_schedule_repeat\" onchange=\"toggle_schedule_repeat();update_vlm_add_schedule();\"
    />"))||(icontains(body,"<input type=\"text\" name=\"sout_mrl\" id=\"sout_mrl\" size=\"60\"
    onkeypress=\"if( event.keycode == 13 ) vlm_output_change();\"/>"))
  method: GET
  
